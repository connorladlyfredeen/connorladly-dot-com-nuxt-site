# Optional: override the Sentry client DSN per environment (e.g. in Netlify).
# A default public DSN is already baked into nuxt.config.ts, so this is only
# needed if you want to point a given deploy at a different Sentry project.
NUXT_PUBLIC_SENTRY_DSN=
